import React from 'react'
import styles from '../Loading/Loading.module.css'

const Loading = (props) => {
    if(!props.show){
        return null
    }
    return (
        <div>
            
        </div>
    )
}

export default Loading